import logger from 'util';

/**
 * exponentiation
 * an cleaner alternative to Math.pow
 */

 const twoExponentialTwoPow = Math.pow(2, 2); // 4

const twoExponentialTwo = 2 ** 2; // 4

logger.log(`Math.pow(2, 2): `, Math.pow(2, 2));
logger.log(`2 ** 2: `, 2 ** 2);
